# V18 分阶段 RoPE：实测优先，后续恢复

从step24400继承全部权重、DPT、Adam状态和EMA，新增1000步至25400。只训练JEPA恢复，位姿冻结、历史关闭。

先以可信实测位置完成4层JEPA和共享DPT粗预测，再用预测深度经crop相机内参反投影，将恢复位置用于共享最后一层的CAD读取，并复算最后一层与最终DPT。测量与恢复互斥，可信实测始终优先。没有新参数或独立decoder，不把预测canonical XYZ通过基础位姿变换当作真实相机坐标。

实测筛选：可见性≥0.7、有效深度比例≥0.5、深度标准差≤0.05d。恢复筛选：物体support≥0.7、几何有效性≥0.5、有限正深度；位置权重为0.5×support×有效性。坐标/置信度在RoPE分支detach；增加0.25倍粗几何监督，其余V17目标保留。阈值是启发式，不能保证几何正确。

21项CPU检查通过。H20真实batch4×40帧预检通过，热身后2.884秒/episode（不含8卡通信/优化器），峰值10.44GB。预检中约4.44%的CAD可用patch走实测、1.75%走恢复，其余回退；这是所有有效patch的比例，不是物体缺失区域覆盖率。测量/恢复平均信任约0.872/0.243。新路径与共享模块梯度有效，teacher/crop参考不变。

24402→24403跨进程恢复及8rank启动审计已通过：源权重逐值一致、Adam状态完整继承、无DPT重置，位姿与历史保持冻结。控制器正在继续到24900；+500/+1000固定40序列评估后，对终止权重再做RoPE开关。复用已封存的V17终止评估作为同协议参考；不会自动扩大预算。

[设计与坐标说明](../../../../docs/STAGED_ROPE_V18.md) · [实时状态](LIVE_STATUS.md) · [H20预检](preflight_receipt.json) · [启动审计](startup_receipt.json)
